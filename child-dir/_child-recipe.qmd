
## `r stringr::str_to_title(get_part_of_recipe(recipes, id, "name"))`

:::: {.columns}

::: {.column width="70%"}

```{r}
get_part_of_recipe(recipes, id, "ingredients") |>
  plain_list() 
```

:::

::: {.column width="10%"}

:::

::: {.column width="20%"}

```{r}
#| output: asis

get_detail(recipes, id, "temperature")
```

```{r}
#| output: asis

get_detail(recipes, id, "time")
```

```{r}
#| output: asis

get_detail(recipes, id, "size_and_quantity")
```

:::

::::

```{r}
#| output: asis

get_part(recipes, id, "method")
```

```{r}
#| output: asis

get_part(recipes, id, "notes")
```

```{r}
#| output: asis
number_pictures <- get_part_of_recipe(recipes, id, "pictures") |>
  as.numeric()
  
numbers <- 1:number_pictures
numbers <- ifelse(nchar(numbers) < 2, paste0("0", numbers), numbers)
  
if(number_pictures > 0){
  purrr::map_chr(numbers, \(number) {
  knitr::knit_child(input = "child-dir/_child-picture.qmd",
                        envir = environment(),
                        quiet = TRUE) }) |>
  cat(sep = '\n')
} 
```
